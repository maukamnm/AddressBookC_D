package com.munoz;
import java.util.Comparator;
public class comparator  implements Comparator<Address>{
	@Override   
	public int compare(Address o1, Address o2) {
	        return o1.getLname().compareTo(o2.getLname());
	    }
	@Override
	public String toString() {
		return "Sort";
	}
}
